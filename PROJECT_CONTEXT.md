# VLA-RelComp 固定项目上下文

## 学习者与环境

学习者会一点 C/Python，但没有完整 AI、深度学习、Linux、Git、PyTorch、机器人或科研项目经验。教材按接近零基础编写：先给具体可观察场景，再引入解决当前问题所需的理论。当前从普通个人电脑和免费 CPU 环境起步；后半段可以教授 Linux/NVIDIA 与云 GPU，但未经用户明确授权不购买、不租用、不启动收费资源。

## 最终目标

70 天后，学习者能够沿教程独立完成：基础工具 → PyTorch → Transformer/多模态/VLA → VLA-Arena `PrepositionCombinations` → L0/L1/L2 基线 → 行为级诊断 → 最小修复与消融 → 图表、报告、复现说明和答辩。

## 冻结研究问题

在 VLA-Arena 的未见空间关系组合上，VLA 的主要行为瓶颈在哪里？能否让诊断证据触发一个只使用 L0 数据的最小修复，提高 L1/L2，同时保持 L0？

- 官方锚点：VLA-Arena（ICML 2026）。
- 唯一目标套件：`PrepositionCombinations`；冻结资料记录的代码注册名为 `extrapolation_preposition_combinations`。
- 候选基线：SmolVLA 与 OpenVLA；真正选择以日后实际 L0 能力和执行稳定性为准。
- 当前没有运行 VLA-Arena、下载模型、租 GPU、训练或得到真实成绩。

## L0 / L1 / L2 的课程口径

- **L0**：训练分布内或基础组合；用于建立能力基线，并且是修复训练允许使用的数据范围。
- **L1**：出现未见的局部组合，用于组合泛化评测。
- **L2**：更强的未见组合，用于更困难的泛化评测。

具体任务划分必须以课程锁定的 VLA-Arena 版本与上游定义为准。L1/L2 严格保留为测试，不参与训练、选超参数和 checkpoint 早停。

## 四段行为链

1. 目标接近/接触：行为是否指向正确可动物体；不能据此断言内部识别机制。
2. 抓取/抬升：正确目标与夹爪接触并离开支撑面达到阈值。
3. 搬运到参照区域：目标与正确参照物或目标区域的距离持续下降。
4. 终态关系满足：CBDDL goal predicate 返回成功；它不自动证明前三段都正确。

## L0/L1/L2 实验主线

- E0 基础设施对照：random/无模型策略验证环境、初始状态、视频和 success 判定。
- E1 官方模型基线：分别做最小 pilot，按任务而非只按宏平均报告。
- E2 匹配反事实：尽量固定场景、对象、初始状态、seed、模型和推理设置，每次只改变一个有效关系或组合。
- E3 诊断 oracle：语言规范化 oracle 与使用仿真真值的可撤销视觉对象提示，仅用于定位瓶颈。
- E4 最小修复：由 E2/E3 证据选择单一轻量改动；仅使用 L0 训练，再评测 L0 保持与 L1/L2 泛化。

## 指标与证据

保留每个 run/episode 的版本、模型 revision、suite、level、task、seed/init state、指令、干预、目标/参照物/关系、四段事件、success、步数、墙钟、显存、视频/日志/结果路径和异常。核心统计包括任务级成功率、Wilson 区间、阶段转化率、pair asymmetry、intervention recovery/damage，以及合适的配对检验。字段未知时留空，不猜测补齐。

## 创新与表述边界

仅复跑分数、展示注意力图/失败视频、普通提示词改写、无机制调参、从头训练大模型或只做 Demo 都不是本项目的独立增量。研究增量来自匹配反事实、四段状态事件、受控 oracle 和由诊断触发的最小修复。即使修复不涨点，只要诊断、对照、失败边界和复现证据可靠，也可以形成可信结果。
