# 教材 V2 编写规则

## 1. 先做项目，再即时补课

每一天从一个可验收的真实 VLA-RelComp 产物开始。只解释完成该产物当前缺少的知识；诊断已通过的内容允许跳过。旧基础模块仅从 `foundation_library/` 按需调用。

## 2. 每日十段模板

正文标题与顺序固定如下：

1. **真实项目产物**：文件名、证据和完成定义。
2. **当前卡点**：为什么今天的项目任务会卡住。
3. **前置诊断**：5–15 分钟可判定，失败时精确路由到 F01–F18。
4. **即时知识**：只补完成当天任务所需的最少概念。
5. **成熟材料处方**：最多 1 个主材料和 1 个补充材料，精确到章节/页面/预计时长；中文优先。视频时间段必须实际核对。
6. **最小实验**：15–40 行、免费、本地、完整注释，可单独运行。
7. **真实 VLA-Arena 操作**：锁定 commit 的真实路径、函数、配置、命令及预期证据；无法运行时明确标记。
8. **独立挑战**：新输入或新任务，不给完整步骤，答案与正文分离。
9. **验收 rubric**：机器命令、产物清单和口述评分标准。
10. **证据复盘**：区分已运行事实、静态源码事实、fixture 结果、待 GPU 验证和不能推出的结论。

## 3. 项目引用与锁定版本

上游引用必须使用完整 commit URL，例如：

`https://github.com/PKU-Alignment/VLA-Arena/blob/babe582ebffc82b979b77964a7e56417d02f63a4/<path>#Lx-Ly`

引用至少精确到文件和函数；涉及配置时同时写出配置路径和关键字段。不得用 `main` 分支页面替代锁定事实。

## 4. 代码与证据

- 教学代码放在当天 `code/`；测试放在 `tests/`；共享 fixture、schema、脚本和答案分别放在 `shared/` 对应目录。
- 示例必须有中文注释、固定输入、明确输出和非零失败退出码。
- 默认写入 `learner_outputs/mainline/dayNN/`，不得覆盖 `shared/fixtures/`。
- 免费 smoke test 必须实际执行；未运行的真实环境命令不能写成“测试通过”。
- 不运行付费 GPU，不更改上游或研究仓库。

## 5. Gate 和答案

每个 Gate 使用陌生输入，包含允许/禁止材料、机器验收、产物、口述 rubric，以及“通过 / 补做 / 停止扩张”三种结论。参考答案只放 `shared/answer_keys/`；首次挑战正文不得泄露。

## 6. 进度口径

“教材文件已编写”与“学习者完成课程”是两件事。Day 0、F01–F18 和代表性样章均不增加主线已完成天数。只有学习者按 rubric 留下真实证据后，才可记录相应 Day 完成。
