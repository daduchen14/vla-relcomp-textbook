# 教材统一写作规则

## 每日固定结构

每个 `dayNN/README.md` 必须包含以下内容，标题可为行文需要略作调整，但不得缺项：

1. **今天学什么概念**：从直觉、用途、正式含义和与 VLA-RelComp 的关系讲清楚。
2. **今天做什么**：所有命令从仓库根目录开始，可复制，写明文件位置、操作和预期输出。
3. **完整代码**：先展示或编写约 15—40 行的最小可运行版本，再逐步扩展到完整版本。自编代码存入 `dayNN/code/`；重要语句有相邻中文注释。正文完整展示短代码，较长代码可逐段导读并链接完整文件。
4. **动手环节**：明确“先预测什么、改哪个变量、运行哪条命令、预期怎样变化、结果说明什么”。
5. **检查点**：3—5 道概念与代码自测题，题后紧跟可核对的标准答案。
6. **外部材料**：精确到官方文档小节、论文章节/页码、视频时间段或上游仓库文件/符号；说明为什么看、看哪里、看完会什么、暂时跳过什么。

每天还必须写明：建议用时、前置知识、最低完成线、标准完成线、当天产物、常见错误及单项止损时间。正文通常约 5000—10000 个中文字符，以讲清为准。

## 教学顺序

采用同一闭环：为什么现在需要 → 直觉 → 正式定义 → 最小例子 → 最小可运行代码 → 工程扩展 → 改一个变量 → 连接项目 → 自测与产物。不能用外链代替主干讲解，也不能第一步给初学者数百行成品。

## 代码与数据标准

- Python 默认兼容 3.10+；优先标准库，新增依赖须解释用途、固定方式和免费安装命令。
- 路径使用 `pathlib`，输出默认放入已忽略的 `learner_outputs/dayNN/`；源码、fixture 与个人产物分开。
- 提供 `--help` 或清晰入口；错误必须给可操作信息，避免静默吞错。
- 所有教学 CSV/JSON 行的标识以 `fixture_` 开头；输出显著声明 `synthetic teaching data; not a VLA result`。
- 当前机器可免费运行的脚本必须实际执行；至少做语法检查和一次正常路径 smoke test，必要时再测失败路径。
- GPU 章节要给完整命令、配置、预期现象、故障处理和证据保存方法，但明确“当前未运行”，不得伪造数值。
- 不复制大段第三方源码；写本教程自己的最小实现，并指向上游精确文件、类或函数。

## 科研真实性与语言边界

- 区分真实事实、教学类比、fixture 结果、预期现象和未运行步骤。
- 只依据行为、状态事件和受控干预作有限推断，不声称模型“真正理解”或“完全不理解”。
- L1/L2 不用于训练、超参数选择或早停；oracle 的仿真真值不能进入最终部署修复。
- 结果按 task/level/seed 保留原始计数；统计显著性不能替代效应量、置信区间与失败案例。

## 日与阶段之间的连续性

每课开头指出使用了哪些前课产物，结尾写明当天产物将供哪一课使用。阶段末更新根 README，并检查链接、命令、fixture 命名和状态是否一致。这里只检查教材连续性，不复核被冻结的研究决策。
